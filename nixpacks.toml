[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.install]
cmds = [
    "cd frontend && npm install",
    "pip install -e ."
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "uvicorn llm_listener.api:app --host 0.0.0.0 --port ${PORT:-8000}"
